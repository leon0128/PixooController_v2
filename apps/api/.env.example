# Only used when running the API from the host (outside the container).
# Inside the container, DATABASE_URL is injected by docker-compose.yml and points at `db`.
DATABASE_URL="postgresql://pixoo:pixoo@localhost:5432/pixoo"
